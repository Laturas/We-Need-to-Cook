using System.Collections.Generic;
using UnityEngine;

public class Government : MonoBehaviour
{
    public List<WorkOrder> outstandingOrders;
    // Start is called before the first frame update
    void Start()
    {
        //outstandingOrders.Insert(new WorkOrder());
    }
}
